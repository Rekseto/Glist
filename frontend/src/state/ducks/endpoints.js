const domain = "http://192.168.1.16:3000";

// auth

export const loginEndpoint = `${domain}/auth/login`;
export const logoutEndpoint = `${domain}/auth/logout`;
export const verifyEndpoint = `${domain}/auth/verify`;
export const registerEndpoint = `${domain}/auth/register`;

// metrics
export const metricsEndpoint = `${domain}/`;
